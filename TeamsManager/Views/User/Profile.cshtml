@{ ViewBag.Title = "Profile"; }

@* Pagina Profile pentru utilizator *@

<div class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            @Html.ActionLink("Teams Manager", "Index", "User", new { area = "" }, new { @class = "navbar-brand" })
        </div>
        <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
                <li>@Html.ActionLink("Home", "Index", "User")</li>
            </ul>
        </div>
    </div>
</div>

<h2>Profile</h2>

<hr />

<fieldset>
    <legend>User DashBoard</legend>
    @* Verificam daca suntem logati si  afisam detaliile utlizatorului *@
    @if (Session["UserName"] != null && Session["UserID"] != null)
    {
        <text>
            ID: @Session["UserID"].ToString() Usrename: @Session["UserName"].ToString() Email:
        </text>
    }
</fieldset>

<hr />

@* Oferim utilizatorului posibiliatea de a isi sterge contul *@
<div class="item bottom" onclick="location.href='@Url.Action("Delete", "User")'">Delete</div>